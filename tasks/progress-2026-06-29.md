# 项目进度记录

> 最后更新：2026-06-29
> 当前分支：`ai/p1-finance-adapter`（与 master 同步）
> origin/master：已全部推送，工作区干净
> 协作模式：Claude Code（主控/审查）+ Codex（实现）

---

## 一、总体进度

### 阶段完成情况

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0 上线阻断修复（P0-01~P0-10） | ✅ 全部合入 master | 事实源、持久化、安全、迁移、真实依赖 E2E |
| P1 验收增强（P1-01~P1-05） | ✅ 全部合入 master | Connector 合约、目录治理、质量报告、监管报表、财务适配 |
| P2 生产强化（P2-01~P2-05） | ⬜ 未开始 | 大表分区、压测、故障演练、安全扫描、备份恢复 |

### P1 阶段提交记录（均已推 origin）

| 任务 | 提交 | 主题 |
|---|---|---|
| P1-01 | `c94e6bcd` | SourceConnector 合约 + 能力矩阵 + checkpoint 持久化 |
| P1-02 | `1bd553dc` | 目录治理：血缘、质量摘要、使用统计 |
| P1-03 | `7d67bd96` | 质量报告持久化 + 维度生成 + 导出 |
| P1-04 | `96ada854` | 监管报表：持久化、脱敏、报送回执 |
| P1-05 | `807995e1` | 财务适配：同步持久化、重试、采购适配器 |

最新迁移版本：**V021**（t_finance_sync_record）

---

## 二、P1 各任务交付摘要

### P1-01 Connector 合约 ✅
- SourceConnector 规范（协议/格式/同步模式/断点续传/限流能力声明）+ 8 适配器能力矩阵 + 每类协议 contract test。
- 借鉴 Airbyte。

### P1-02 目录治理 ✅
- 目录血缘（资产→服务/接入任务）、质量摘要（按资产聚合）、使用统计。
- 借鉴 DataHub。

### P1-03 质量报告 ✅（含返工）
- t_quality_report（V019）+ 按 PARTNER/ASSET/SERVICE 维度生成 + 导出 Blob。
- 返工修复 5 个 P2：ObjectMapper 序列化、parseInstant 校验、404 映射统一、equals 精确匹配、无匹配 score=0。
- 借鉴 Great Expectations。

### P1-04 监管报表 ✅（含返工）
- t_regulatory_report（V020）+ 三类型生成 + PERSONAL_INFO 脱敏 + 报送回执入库（SUBMITTED/FAILED）。
- 返工修复 2 个 P2：移除 apiKey 字段（消除凭证暴露）、审计 traceId 串联。
- 借鉴数据报送平台 + Lago §9。

### P1-05 财务适配 ✅
- t_finance_sync_record（V021）+ 推送回执入库（SUCCESS/FAILED）+ 重试（retryCount 累加）+ 采购适配器框架。
- 借鉴 Lago §9（FinanceSyncAdapter/PurchaseContractAdapter/FinanceSyncRecord）。
- **遗留 2 个 P2（不阻断）**：见下文待办。

---

## 三、待办事项（明天继续）

### P1-05 返工（不阻断，建议处理）
审查报告：`reviews/claude-review-P1-05.md`

1. **P2-1**：异常码 `BILL_STATE_INVALID` 改为 `BILL-409`（与 `BILL-404` 后缀规约一致），移除 `GlobalExceptionHandler` 的 magic string 特判。同步更新 `BillingController.statusChange`、`BillStateMachine`、`FinanceSyncService`、MockMvc/单元测试断言。
2. **P2-2**：明确 retryCount 语义（最近失败序列计数 vs 累计），补测试覆盖"sync FAILED→retry SUCCESS→sync 又 FAILED→retry"跨序列场景。
3. P3-2：真实对接时 adapter 异常 message 截断 + 脱敏（待 Q-06）。

### P1 阶段收尾
4. **P1 验收材料汇总**（`docs/development-process-workflow.md` §6 完成判定要求每阶段输出验收材料）：5 任务可演示产出 + 测试 + 证据。
5. P1 期间持续跟踪的 P0 遗留（`tasks/claude-plan-P1.md` 附）：
   - P0-08 H-1：DB REVOKE UPDATE/DELETE ON t_audit_log runbook（上线前）
   - P0-10 V015 checksum：已执行旧 V015 的库 flyway:repair（上线前）
   - P0-10 E2E：补跑 e2e-p0.sh 附 curl + 重启恢复证据（上线前）

### 下一阶段：P2 生产强化（待启动）
6. P2-01 大表分区归档（t_service_invoke_log/t_audit_log/t_raw_data 分区 + 查询优化）
7. P2-02 压测容量（JMeter，P95/P99、TPS、批量接入）
8. P2-03 故障演练（关键依赖故障注入 + 恢复）
9. P2-04 安全扫描（SCA/渗透测试）
10. P2-05 备份恢复（备份、恢复、审计校验）

---

## 四、关键约定与流程

- **分支约定**：每任务独立分支 `ai/p1-<主题>`，审查通过后 ff 合并 master。P1-05 因改动落在 master 工作区，已用 `git branch -f` 把 `ai/p1-finance-adapter` 重指向提交。
- **任务文档**：`tasks/claude-plan-PX-XX.md`（第一性原理计划）+ `tasks/codex-task-PX-XX.md`（Codex 任务单）。
- **审查文档**：`reviews/claude-review-PX-XX.md`（+ 返工复查）。
- **审查规则**：CLAUDE.md §7 + §7.1 对抗式审查（安全/并发/金额/持久化/外部依赖场景必做）。
- **国产化**：新表 MySQL + 达梦双库（db/migration + db/migration-dm），MigrationDialectCompatibilityTest 守护，避开 ` LIMIT `/DM ` TEXT`/` TINYINT ` 方言。
- **借鉴**：`docs/github-reference-functional-design.md`（Keycloak/Airbyte/Kong/DataHub/Great Expectations/Ranger/MinIO/Lago/Superset），只借鉴领域模型不引入依赖。

---

## 五、明天恢复指引

1. 确认仍在 `ai/p1-finance-adapter` 分支（或切 master）。
2. 优先处理 P1-05 返工 P2-1/P2-2（派发 Codex 任务单 → 实现 → 审查 → 合并）。
3. 或汇总 P1 验收材料。
4. 或启动 P2-01（生成 `tasks/claude-plan-P2-01.md` + `tasks/codex-task-P2-01.md`）。

参考文档：`docs/development-process-workflow.md`（流程）、`tasks/claude-plan-P1.md`（P1 总规划）、`docs/requirements.md`（需求）。
